import reducer, { initialState } from './reducer';
import * as actions from './actions';
import * as dispatchers from './dispatchers';

export default { reducer, initialState, actions, dispatchers };
